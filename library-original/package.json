{
    "name": "library-original",
    "version": "1.0.0",
    "license": "ISC",
    "main": "dist/index.js",
    "scripts": {
      "build": "tsc",
      "clean": "rimraf dist .parcel-cache dist-tsc",
      "rebuild": "yarn clean && yarn build && yarn build:tsc"
    },
    "devDependencies": {
      "rimraf": "^3.0.2",
      "typescript": "^4.4.4"
    }
  }
  